#!/bin/sh

curdir=$( pwd )

for pkg in paula.*; do
    pkgdir=${curdir}/${pkg}
    if test -d ${pkgdir}/externals; then
	cd ${pkgdir}/externals
	rm -f paula.*
	# pkglist.txt should be generated from setup.py
	for k in `cat pkglist.txt`; do
	    ln ../../${k} . -s
	done
    fi
done
